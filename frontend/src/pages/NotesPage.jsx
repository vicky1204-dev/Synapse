import PageHeader from "../components/PageHeader";

const Notes = () => {
  return (
    <div className=" min-h-screen p-8 w-full bg-black">
      <PageHeader title={"Notes"} />
    </div>
  );
};

export default Notes;
